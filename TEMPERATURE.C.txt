#include <stdio.h>

int main() {
    float celsius = 25.0; 
    float fahrenheit;

    fahrenheit = (celsius * 9 / 5) + 32;

    printf("Temperature in Celsius: %.2f\n", celsius);
    printf("Temperature in Fahrenheit: %.2f\n", fahrenheit);

    return 0;
}

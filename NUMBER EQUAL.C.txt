#include <stdio.h>

int main() {
    int num1, num2;

    // Taking input from the user
    printf("Enter first number: ");
    scanf("%d", &num1);

    printf("Enter second number: ");
    scanf("%d", &num2);

    // Checking if both numbers are equal
    if (num1 == num2) {
        printf("The two numbers are equal.");
    } else {
        printf("The two numbers a


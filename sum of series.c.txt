#include <stdio.h>

int main() {
    int n, i;
    double sum = 0.0;

    if (scanf("%d", &n) != 1 || n <= 0) {
        printf("Invalid input\n");
        return 1;
    }
    for (i = 1; i <= n; i++) {
        sum += 1.0 / i;
    }
    printf("%.6lf\n", sum);

    return 0;
}
